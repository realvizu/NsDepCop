<Project>

  <!-- Adds the config.nsdepcop file as an additial analyzer file if it exists. -->
  <ItemGroup>
    <AdditionalFiles Include="$(ProjectDir)config.nsdepcop" Condition="Exists('$(ProjectDir)config.nsdepcop')" />
  </ItemGroup>

</Project>